// import axios from "axios";

// export default function makePaymentMobile(number, pin, amount) {
//   return axios.post("URL", { number, pin, amount });
// }

// export default function makePay(number, pin, amount) {
//   return axios.post("URL", { number, pin, amount });
// }
